# JPA and DataSource Configuration
spring.jpa.database=POSTGRESQL
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate Configuration
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=none

# Dictionary Configurations
lez.rus=lezgi_rus_dict_babakhanov_v2.json
rus.lez=rus_lezgi_dict_hajiyev_v2.json
lez.eng=lezgi_eng_dict_rasim_rasulov.json
dialect.dict=legi_dialect_dict.json

################################################### NOTE ######################################################
# Moved properties below to env file
# not the best way to do this, but it works for current Spring usage
# next time either use appropriate Spring Boot or use Hibernate directly for db connection if API is not needed
###############################################################################################################
#spring.datasource.url=jdbc:postgresql://localhost:5433/gafargan_bot_db
#spring.datasource.username=docker
#spring.datasource.password=docker